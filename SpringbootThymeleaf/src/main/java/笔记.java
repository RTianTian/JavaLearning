public record 笔记() {
    /*
    *1.ctrl + f 是搜索框
    *2.session.setAttribute（键对值，value值）  同hashMap()中的push，set方法一样
    *3.  StringUtils.hasLength 如果字符序列不为null值，且字符序列大于0，则返回true值
    * 4. model.addAttribute(Map attribute)如果变量已存在，则覆盖
    * 5.  model.mergeAttribute(Map attribute)如果变量已存在，则忽略
    *
    *
    *
    * */
}
